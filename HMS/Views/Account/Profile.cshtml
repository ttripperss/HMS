@* @model HMS.Models.User
@{
    ViewData["Title"] = "Profile";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container mt-5">
    <div class="row">
        <div class="col-md-4">
            <img src="@Model.ProfilePictureId" alt="Profile Picture" class="img-fluid rounded-circle" style="width: 200px; height: 200px; margin-bottom: 20px;">
        </div>
        <div class="col-md-8">
            <h2 class="mb-3">User Profile</h2>
            <dl class="row">
                <dt class="col-sm-4">Username:</dt>
                <dd class="col-sm-8">@Model.UserName</dd>
                <dt class="col-sm-4">Email:</dt>
                <dd class="col-sm-8">@Model.Email</dd>
                <dt class="col-sm-4">First Name:</dt>
                <dd class="col-sm-8">@Model.FirstName</dd>
                <dt class="col-sm-4">Last Name:</dt>
                <dd class="col-sm-8">@Model.LastName</dd>
            </dl>
            <a asp-controller="User" asp-action="Manage" class="btn btn-primary">Manage Account</a>
        </div>
    </div>
</div>
 *@