@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@*
    @{

    <div class="text-center">
        <h1>Contact Us</h1>
        <p>Phone: 03024354896</p>
        <p>Email: hotemmangment@gmail.com</p>
    </div>
} *@

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Us - HMS</title>
    <link rel="stylesheet" href="~/css/Contact.css">
    <style>
        /* Background Image */
        body {
            font-family: Arial, sans-serif;
            background-image: url('/images/Untitled%20design.jpg'); /* Corrected path */
            background-size: cover; /* Ensures the background image covers the entire body */
            background-position: center center; /* Ensures the image is centered */
            background-attachment: fixed; /* Makes the background fixed when scrolling */
            background-repeat: no-repeat; /* Prevents tiling of the background image */
            margin: 0;
            padding: 0;
            color: #333;
        }

        /* Section Title Animation */
        .section-title {
            text-align: center;
            font-size: 3em;
            margin-bottom: 20px;
            color: black; /* Bold black color */
            font-weight: bold; /* Makes the text bold */
            position: relative;
            opacity: 0;
            animation: fadeIn 1.5s ease-out forwards;
            animation-delay: 0.2s;
        }

        .section-title::after {
            content: "";
            width: 80px;
            height: 4px;
            background-color: #3498db;
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
        }

        /* Fade-In Animation */
        @@keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(20px);
            }

            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Contact Info Section */
        .contact-info {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            margin-bottom: 30px;
            gap: 20px;
            opacity: 0;
            animation: fadeIn 1.5s ease-out forwards;
            animation-delay: 0.5s;
        }

        .contact-card {
            flex: 1;
            min-width: 280px;
            background-color: rgba(255, 255, 255, 0.9); /* Slight transparency */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2); /* Stronger shadow for prominence */
            text-align: center;
            border: 2px solid #000; /* Black border for contact cards */
            margin-bottom: 20px;
        }

        .contact-card i {
            font-size: 2.5em;
            color: #000; /* Black icons */
            margin-bottom: 10px;
        }

        .contact-card p {
            font-size: 1.1em;
            color: #333; /* Dark color for text */
            font-weight: bold;
        }

        /* Contact Form Animation */
        .contact-form {
            background-color: rgba(255, 255, 255, 0.9); /* White with slight transparency */
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3); /* Darker shadow for more prominence */
            opacity: 0;
            animation: fadeIn 1.5s ease-out forwards;
            animation-delay: 0.8s;
            max-width: 800px;
            margin: 20px auto;
            border: 2px solid #000; /* Black border around the form */
        }

        .contact-form h3 {
            margin-bottom: 20px;
            color: #000; /* Black color for the title */
            font-size: 1.8em;
            text-align: center;
            font-weight: bold;
        }

        /* Form Input Fields */
        .contact-form input,
        .contact-form textarea {
            width: 100%;
            padding: 12px;
            margin-bottom: 15px;
            border: 2px solid #000; /* Black border for input fields */
            border-radius: 5px;
            font-size: 1.1em;
            transition: border-color 0.3s ease;
            color: #333; /* Dark text color for better readability */
        }

        .contact-form input:focus,
        .contact-form textarea:focus {
            border-color: #000; /* Focus color change to black */
            outline: none;
        }

        /* Contact Form Submit Button */
        .contact-form button {
            background-color: #000; /* Black background for button */
            color: #fff; /* White text on the button */
            padding: 12px 20px;
            border: none;
            border-radius: 5px;
            font-size: 1.2em;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%; /* Full-width button */
        }

        .contact-form button:hover {
            background-color: #333; /* Darker black shade on hover */
            transform: scale(1.05); /* Slight increase in button size on hover */
        }

        /* Map Section Animation */
        .map {
            margin-top: 30px;
            text-align: center;
            opacity: 0;
            animation: fadeIn 1.5s ease-out forwards;
            animation-delay: 1s;
        }

        .map iframe {
            border: none;
            width: 100%;
            height: 400px;
            border-radius: 10px;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="section-title">Contact Us</h1>

        <!-- Contact Info Section -->
        <div class="contact-info">
            <div class="contact-card">
                <i class="fa fa-map-marker" aria-hidden="true"></i>
                <p><strong>Address:</strong> 123 Health Street, YourCity, YourCountry</p>
            </div>
            <div class="contact-card">
                <i class="fa fa-phone" aria-hidden="true"></i>
                <p><strong>Phone:</strong> +1 (123) 456-7890</p>
            </div>
            <div class="contact-card">
                <i class="fa fa-envelope" aria-hidden="true"></i>
                <p><strong>Email:</strong> contact@hospital.com</p>
            </div>
        </div>

        <!-- Contact Form -->
        <div class="contact-form">
            <h3>Send Us a Message</h3>
            <form>
                <label for="name">Your Name</label>
                <input type="text" id="name" placeholder="Enter your name" required>

                <label for="email">Your Email</label>
                <input type="email" id="email" placeholder="Enter your email" required>

                <label for="message">Your Message</label>
                <textarea id="message" placeholder="Write your message" required></textarea>

                <button type="submit">Send Message</button>
            </form>
        </div>

        <!-- Map Section -->
        <div class="map">
            <h3>Our Location</h3>
            <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3402.451490959983!2d74.29428607422987!3d31.48427154892704!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x3919030008901951%3A0xfe6e8940c5949447!2sFatima%20Jinnah%20Medical%20University!5e0!3m2!1sen!2s!4v1630565237873!5m2!1sen!2s"
                allowfullscreen="" loading="lazy"></iframe>
        </div>
    </div>
</body>

</html>


